package maps;

import org.openqa.selenium.By;

public class TransportRentMap {

    //Buttons
    public By continueButton = By.xpath("//button[contains(@class, 'transport__cta') and contains(text(), 'Continue')]");
}